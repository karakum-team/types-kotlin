name: Package Lock

on:
  push:
    branches:
      - main

concurrency:
  cancel-in-progress: true

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Update
        uses: karakum-team/vitalik@v0.8
